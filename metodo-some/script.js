// o método some testa se ao menos um dos elementos no array passa na condição e retorna um valor true or false

// exemplo de array de idades

const ages = [15, 19, 65, 14, 89];

const results = ages.some((age) => age < 18);

console.log(results);
